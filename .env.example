# Hedera Relay Proxy Configuration
# Copy this file to .env and update the values

# Server Configuration
PORT=3000
DB_FILE=routing_db.json
DEFAULT_SERVER=https://mainnet.hashio.io/api

# Hedera Consensus Service Configuration
# Required for Hedera functionality
HEDERA_ACCOUNT_ID=0.0.123456
HEDERA_PRIVATE_KEY=302e020100300506032b657004220420...
HEDERA_NETWORK=testnet

# Optional: Existing topic ID (if you already have one)
# HEDERA_TOPIC_ID=0.0.654321

# Example routing configuration (these are loaded from routing_db.json)
# You can also update routes via the POST /routes endpoint
